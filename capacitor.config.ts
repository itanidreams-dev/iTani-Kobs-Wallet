import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.itanidreams.wallet',
  appName: 'iTani Kobs Wallet',
  webDir: 'dist'
};

export default config;
